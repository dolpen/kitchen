server {
  listen 80;
  server_name <%= node['minecraft']['domain']['play'] %>;
  root    /var/www/minecraft;
  index   index.html index.htm;
  location /backup {
    autoindex on;
    autoindex_localtime on;
  }
}
